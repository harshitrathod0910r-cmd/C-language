#include<stdio.h>
int main()
{
    int week;
    printf("enter week no");
    scanf("%d",&week);
    switch(week)
    {
        case 1:
            printf("sunday");
            break;
        case 2:
            printf("sunday");
            break;
        default:
            printf("no week");

    }
    return 0;
}